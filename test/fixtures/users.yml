admin:
    name: Admin User
    email: fixture@example.com
    password_digest: <%= User.digest('password') %>
    admin: true

read:
    name: Read-Only User
    email: dutchess@example.gov
    password_digest: <%= User.digest('password') %>