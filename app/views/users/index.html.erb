    <h2><%= @title[:subtitle] %> (<%= @users.count %>)</h2>

    <table class="app_index">
      <thead>
        <tr>
          <th>User</th>
          <th>Currently Reading</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
      <% @users.each do |user| %>
        <%= render user %>
      <% end %>
      </tbody>
    </table>
