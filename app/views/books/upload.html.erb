<% provide(:title, "Upload #{@book.title}") %>

<h2>Upload</h2>
<h3><%= @book.title %></h3>

<%= form_for(:upload, url: upload_review_path) do |f| %>

    <p>Your file should be an HTML document containing your entire book test marked up with HTML5 and the classes and tags defined in 1_books.scss.</p>
    <p>The inner HTML of each section element in your file will be saved automatically in a separate section.</p>

    <%=  f.hidden_field :book_id, :value => @book.id %>

    <p>
        <%= f.label :ebook_file %>
        <%= f.file_field :ebook_file, { required: true } %>
    </p>

    <p>
        <%= f.label :auto_assign_chapter_nums, "Assign chapter numbers automatically?" %>
        <%= f.check_box :auto_assign_chapter_nums %>
    </p>

    <p>
        <%= f.submit "Upload", class: "btn" %>
    </p>
	
    <p class="form-nav-links">
        <%= link_to "Back to Manage Books", books_path %> | <%= link_to "Go to #{@book.title}", book_path(@book) %>
    </p>
<% end %>